.venv/
venv/
__pycache__/
*.py[cod]
.DS_Store

# Root-level spreadsheets (keep repo root to app trio + tooling)
/*.xlsx

# Web app temp files (keep folders via .gitkeep)
uploads/*
!uploads/.gitkeep
outputs/*
!outputs/.gitkeep

# Local usage counter database
/data/
